# cd to the parent folder of script
cd "$(dirname -- "$(readlink -f "${0:A}")")/.."


PYTHON=python3
SUDO=sudo

if [ "$(uname)" = "FreeBSD" ]; then
  echo "This is FreeBSD."
  PYTHON=python3.12
  pkg install -y rust
fi


rm -rf venv
$PYTHON -m venv venv
./venv/bin/pip3 install -r conf/requirements.txt
